server:
  port: 8081
  error:
    include-message: always

spring:
  application:
    name: ms-books-catalogue

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  # Elasticsearch: usado por el buscador (endpoint /api/books/search)
  elasticsearch:
    uris: ${ELASTICSEARCH_URIS:http://localhost:9200}

  datasource:
    url: jdbc:mysql://195.35.61.36:3306/u301187585_relatos_papel?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: u301187585_relatos_admin
    password: v$Z2!#Xi+v

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

file:
  upload-dir: ./uploads/book-covers

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
